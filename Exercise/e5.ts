const quote = "Imagination is more important than knowledge.";
const author = "Albert Einstein";

const famous_person = author;

const message = (`${author} once said, "${quote}" `);

console.log(message);